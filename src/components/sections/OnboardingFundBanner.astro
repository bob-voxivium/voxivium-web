---
import OnboardingFundLiveStatus from './OnboardingFundLiveStatus.astro'

// Landing-page Voter Onboarding Fund banner. Thin wrapper around the live
// status card with banner-variant chrome. Sits above the fold so the
// brand promise ("we cover the cost of onboarding voters; here's how
// much runway we have") is visible at first paint.
---

<section class="container mx-auto max-w-4xl px-6 pt-6">
  <OnboardingFundLiveStatus variant="banner" />
</section>
